{
  "compilerOptions": {
    // ... (todas as suas outras opções continuam aqui)
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],
  // ========== A CORREÇÃO ESTÁ AQUI ==========
  "exclude": [
    "node_modules",
    "supabase" // Adiciona a pasta supabase para ser ignorada pelo TypeScript do Next.js
  ]
  // ===========================================
}